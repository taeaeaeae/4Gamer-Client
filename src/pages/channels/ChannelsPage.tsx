import ChannelContainer from "../../components/channels/channelContainer";

const ChannelPage = () => {
    return (
        <div>
            <ChannelContainer />
        </div>
    );
};

export default ChannelPage;