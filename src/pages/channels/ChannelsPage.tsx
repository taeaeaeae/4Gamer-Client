import ChannelContainer from "../../components/channels/ChannelContaner";

const ChannelPage = () => {
    return (
        <div>
            < ChannelContainer />
        </div>
    );
};

export default ChannelPage;